include_directories(${CMAKE_SOURCE_DIR}/src )


########### next target ###############

SET(pHash-jni_LIB_SRCS
   dummy.cpp
)

file(WRITE dummy.cpp "//autogenerated file by cmake\n")
add_library(pHash-jni SHARED ${pHash-jni_LIB_SRCS})

target_link_libraries(pHash-jni)

set_target_properties(pHash-jni PROPERTIES VERSION 4.2.0 SOVERSION 4)
install(TARGETS pHash-jni DESTINATION lib)


########### install files ###############

install(FILES  pHash-jni.h DESTINATION include)



#original Makefile.am contents follow:

#INCLUDES = -I$(top_srcdir)/src
#lib_LTLIBRARIES = libpHash-jni.la
#libpHash_jni_la_SOURCES = pHash-jni.cpp
#include_HEADERS = pHash-jni.h pHash_MVPTree.h
#EXTRA_DIST = org/phash/*.java README
#
#libpHash_jni_la_LIBADD = $(top_srcdir)/src/libpHash.la
#
